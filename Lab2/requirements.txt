# Local pipeline dependencies.
#
# Airflow is deliberately NOT here. It pins hundreds of transitive versions and
# installing it alongside MLflow makes pip backtrack for minutes or fail
# outright. Airflow runs in its own image, built from docker/airflow.Dockerfile.

# Data and ML
pandas==2.2.3
numpy==2.2.1
scikit-learn==1.6.0
joblib==1.4.2
pyarrow==18.1.0

# Experiment tracking and model registry (client + local file store)
mlflow==2.19.0

# Configuration
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-cov==6.0.0
