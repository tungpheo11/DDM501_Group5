# =============================================================================
# Credit Default Risk Scoring API
# DDM501 - Lab 1: First ML Product
#
# TODO: Complete the service definition.
# =============================================================================
services:
  # ---------------------------------------------------------------------------
  # TODO: Configure the api service
  # ---------------------------------------------------------------------------
  # Requirements:
  #   build         from the current directory
  #   ports         8000 on the host -> 8000 in the container
  #   volumes       ./models mounted read-only at /app/models
  #   environment   MODEL_PATH=/app/models/credit_model.joblib
  #                 MODEL_VERSION=1.0.0
  #                 REVIEW_THRESHOLD=0.30
  #                 DECLINE_THRESHOLD=0.60
  #   restart       unless-stopped
  #

  api:
    build: .
    container_name: credit-risk-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro
    environment:
      MODEL_PATH: /app/models/credit_model.joblib
      MODEL_VERSION: "1.0.0"
      REVIEW_THRESHOLD: "0.30"
      DECLINE_THRESHOLD: "0.60"
    restart: unless-stopped
